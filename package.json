{
  "name": "hoosseinrahimi-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub profile README and the script that generates its metrics dashboard.",
  "scripts": {
    "metrics": "node scripts/generate-metrics.mjs",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "engines": {
    "node": ">=20"
  }
}
